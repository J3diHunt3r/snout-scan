# Minimal requirements for Stripe billing only
flask==2.3.3
gunicorn==21.2.0
flask-cors==4.0.0
firebase-admin==6.2.0
python-dotenv==1.0.0
stripe==7.0.0

# Note: Pillow removed - not needed for Stripe functionality
# All pet/image processing routes are commented out
